inactive-opacity = 0.65; 
# inactive-opacity-override = false; # Optional, prevents _NET_WM_OPACITY from overriding this value
active-opacity = 0.90;



# Custom window rules
# rules = (
# 	{ match = "_NET_WM_NAME@ *?= 'Alacritty'"; corner-radius = 1; }
# 	# { match = "name = 'firefox'"; shadow = true; },
# 	# ... and so on
# )

opacity-rule = [
  "100:class_g = 'Rofi'",
  "100:class_g = 'REAPER'",
  "87:(name = 'Picture-in-Picture' && class_g = 'zen')",
  "100:fullscreen = true"
];

# animations = ({
# 	triggers = [ "open", "close", "hide" ];
# 	preset = "slide-out";
# 	direction = "left";
# })
